<%= form_for(current_user.active_relationships.find_by(followed_id: @band.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-link btn-block" %>
<% end %>