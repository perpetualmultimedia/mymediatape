<%= form_for(article_photo) do |f| %>
  <% if article_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article_photo.errors.count, "error") %> prohibited this article_photo from being saved:</h2>

      <ul>
      <% article_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :article_id %>
    <%= f.text_field :article_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
